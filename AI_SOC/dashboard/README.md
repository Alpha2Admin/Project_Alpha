# AI-SOC Dashboard

The AI-SOC web dashboard (`dashboard_server.py`) is served from the `cloud-sec-gateway/` directory. This folder is reserved for any standalone frontend assets if the dashboard is decoupled in a future release.

See `cloud-sec-gateway/dashboard/` for the current embedded HTML/CSS/JS dashboard.
