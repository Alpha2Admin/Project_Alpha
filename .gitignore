# ── Environment ──────────────────────
.env
*.env

# ── Python ───────────────────────────
venv/
.venv/
__pycache__/
*.pyc
*.pyo

# ── Logs ─────────────────────────────
logs/
*.log
*.pid

# ── IDE ──────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ── OS ───────────────────────────────
.DS_Store
Thumbs.db

# ── Splunk (local testing artifacts) ─
*.csv

# ── Local Credentials ─────────────────
PORTAINER_CREDS.txt

# ── ML / Fine-tuning ─────────────────
# Model weights are too large for Git (use Git LFS or HuggingFace Hub instead)
models/
*.bin
*.safetensors
*.pt
*.ckpt

# Exported runtime training data (personal examples — do not commit)
training_data/dashboard_export_*.jsonl
training_data/my_samples.jsonl
